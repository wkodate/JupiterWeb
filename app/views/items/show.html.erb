<p id="notice"><%= notice %></p>

<%= render 'adhead' %>

<div align="center">
  <strong><%= @item.title %> / <%= @rss.title %></strong>
</div>

<div align="center">
  <%= image_tag @item.image %>
</p>

<div align="center">
  <%= @item.description %>
</p>

<p>
<%= link_to '続きを読む', @item.link, class: "btn btn-secondary btn-lg btn-block", :target => "_blank" %>
</p>

<%= render 'adtail' %>

<p>
<%= link_to '<< 記事一覧に戻る', root_path %>
</p>

<%= render 'share' %>
